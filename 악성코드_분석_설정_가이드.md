# KVM-VMI 악성코드 분석 환경 설정 가이드

이 저장소는 VMware에서 Ubuntu 22.04를 호스트로 하고 Windows 10을 게스트로 사용하는 악성코드 분석 환경으로 최적화되었습니다.

## 구성 요약

### VMware 설정
- **프로바이더**: VMware Desktop
- **기본 OS**: Ubuntu 22.04 LTS
- **메모리**: 8GB (호스트 VM에 할당)
- **CPU**: 4코어
- **중첩 가상화**: 활성화됨

### Windows 10 게스트 VM 설정
- **RAM**: 4GB
- **vCPU**: 2코어
- **디스크 크기**: 100GB (확장 가능한 qcow2 포맷)
- **저장 위치**: /home/sec/vms/

### 악성코드 분석 최적화 사항

1. **리소스 증가**: Windows 10 VM은 악성코드 실행 시 성능 향상을 위해 4GB RAM과 2개의 vCPU를 가집니다
2. **대용량 디스크**: 샘플 저장 및 분석 산출물을 위한 100GB 디스크
3. **VMI 지원**: 심층 분석을 위한 KVM Virtual Machine Introspection 활성화
4. **네트워크 격리**: libvirt의 기본 네트워크를 통한 VM 격리
5. **스냅샷 지원**: qcow2 포맷으로 분석 전후 스냅샷 생성 가능

### 디렉토리 구조
```
/home/sec/
├── vms/              # VM 디스크 이미지 저장 위치
├── templates/        # VM 빌드를 위한 Packer 템플릿
├── samples/          # 악성코드 샘플 저장소
├── analysis/         # 분석 작업 공간
└── reports/          # 분석 보고서
```

## 설치 및 사용법

### 1. 초기 설정

```bash
cd vagrant
vagrant up --provider=vmware_desktop
```

### 2. VM 접속

```bash
vagrant ssh
```

### 3. Windows 10 VM 시작

```bash
start-win10
# 또는
sudo virsh start win10
```

### 4. 분석 전 클린 스냅샷 생성

```bash
sudo virsh snapshot-create-as win10 clean_state "Clean Windows 10 state before malware analysis"
```

### 5. 악성코드 분석

1. 악성코드 샘플을 `/home/sec/samples/`에 복사
2. Windows 10 VM 시작
3. 네트워크 모니터링 시작:
   ```bash
   monitor-traffic
   ```
4. 악성코드 실행 및 분석

### 6. 분석 후 클린 상태로 복구

```bash
reset-win10
# 또는
sudo virsh snapshot-revert win10 clean_state
sudo virsh start win10
```

## 유용한 명령어

### VM 관리
- `vm-list`: 모든 VM 목록 표시
- `vm-start <vm-name>`: VM 시작
- `vm-stop <vm-name>`: VM 종료
- `vm-destroy <vm-name>`: VM 강제 종료

### Windows 10 전용 단축 명령어
- `start-win10`: Windows 10 시작
- `stop-win10`: Windows 10 종료
- `reset-win10`: 클린 스냅샷으로 복원 후 시작
- `snapshot-win10 <name>`: 새 스냅샷 생성

### 네트워크 모니터링
- `monitor-traffic`: 네트워크 트래픽 캡처
- `monitor-dns`: DNS 쿼리 모니터링
- `monitor-http`: HTTP/HTTPS 트래픽 모니터링

### 스냅샷 관리
- `vm-snapshot <vm-name> <snapshot-name>`: 스냅샷 생성
- `vm-snapshot-list <vm-name>`: 스냅샷 목록 표시
- `vm-snapshot-revert <vm-name> <snapshot-name>`: 스냅샷으로 복원

## 활성화된 VM

현재 설정된 VM (`Vagrantfile`에서 변경 가능):
- Windows 10: **활성화** (악성코드 분석용 메인 VM)
- Windows XP: 비활성화
- Windows 7: 비활성화
- Ubuntu: 비활성화

## 보안 주의사항

1. **항상 스냅샷 사용**: 알 수 없는 악성코드 실행 전 반드시 스냅샷 생성
2. **네트워크 격리**: 악성코드 분석을 위한 네트워크 격리 유지
3. **추가 네트워크 세분화**: 프로덕션 분석 환경에서는 추가 네트워크 세분화 고려
4. **VM 업데이트**: 분석 도구로 Windows 10 VM을 업데이트하되 인터넷으로부터 격리 유지
5. **샘플 관리**: 악성코드 샘플은 암호화된 압축 파일로 저장 (비밀번호: 'infected')

## 분석 도구

호스트 Ubuntu VM에 설치된 도구:
- `tcpdump`: 네트워크 패킷 캡처
- `wireshark`: 네트워크 트래픽 분석
- `gdb`: 디버깅
- `strace`/`ltrace`: 시스템 콜 추적
- `libvmi`: Virtual Machine Introspection 라이브러리

## 문제 해결

### VM이 시작되지 않는 경우
```bash
sudo systemctl restart libvirtd
sudo virsh net-start default
```

### 스냅샷 복원 오류
```bash
sudo virsh snapshot-list win10
sudo virsh snapshot-revert win10 --current
```

### 권한 문제
```bash
sudo usermod -aG libvirt,kvm,libvirt-qemu vagrant
sudo chmod 666 /dev/kvm
```

## 추가 정보

자세한 내용은 영문 문서 `MALWARE_ANALYSIS_SETUP.md`를 참조하세요.

KVM-VMI 프로젝트에 대한 자세한 정보는 [공식 문서](https://kvm-vmi.github.io/kvm-vmi/master/)를 확인하세요.
